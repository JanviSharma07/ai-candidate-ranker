# Optional. If set, JD parsing and explanation polishing use Claude.
# If left unset, the system automatically falls back to rule-based JD
# parsing and skips LLM polishing -- the pipeline still runs fully.
ANTHROPIC_API_KEY=
